Remove library() from global

Add numeric estimate to plot

DT for sorting tables

Design Package:
Function to run the application locally
Argument: a data set

datasets don't have data. before them?
are datasets in other packages e.g. catheter in rmeta

where to put style.css